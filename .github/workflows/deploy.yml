name: Deploy PulseCheck

on:
  push:
    branches: [main]

jobs:

  test:
    name: Django Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: pulsecheck_backend/requirements.txt

      - name: Install dependencies
        working-directory: pulsecheck_backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        working-directory: pulsecheck_backend
        env:
          DJANGO_SETTINGS_MODULE: pulsecheck.settings
          SECRET_KEY: ci-test-secret-key-not-used-in-prod
        run: python manage.py test --verbosity=2

  # 2. Build removed — deployed as Python on Render (no Docker)
  # Render auto-deploys on push to main with:
  #   Build: pip install -r requirements.txt && python manage.py collectstatic --noinput
  #   Start: python manage.py migrate && python create_admin.py && gunicorn pulsecheck.wsgi:application --bind 0.0.0.0:$PORT --workers 3

  # # 3. Deploy (VPS)
  # deploy:
  #   name: Deploy to Server
  #   runs-on: ubuntu-latest
  #   needs: build   

  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Copy prod compose file to server
  #       uses: appleboy/scp-action@v0.1.7
  #       with:
  #         host: ${{ secrets.DEPLOY_HOST }}
  #         username: ${{ secrets.DEPLOY_USER }}
  #         key: ${{ secrets.DEPLOY_SSH_KEY }}
  #         source: "docker-compose.prod.yml"
  #         target: "/opt/pulsecheck"
  #         strip_components: 0

  #     - name: SSH & redeploy
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.DEPLOY_HOST }}
  #         username: ${{ secrets.DEPLOY_USER }}
  #         key: ${{ secrets.DEPLOY_SSH_KEY }}
  #         script: |
  #           cd /opt/pulsecheck
  #           # Move production compose to the active docker-compose.yml
  #           mv docker-compose.prod.yml docker-compose.yml

  #           # Pull latest images from Docker Hub
  #           # Pass DOCKERHUB_USERNAME env if needed, or rely on it stored on server
  #           export DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}
  #           docker compose pull

  #           # Restart services with zero-downtime replacement
  #           docker compose up -d --remove-orphans

  #           # Clean up dangling images to save disk space
  #           docker image prune -f
